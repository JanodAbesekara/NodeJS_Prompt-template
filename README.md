# NodeJS_Prompt-template
Using prompt file give one prompt and build full node js project application 



# prompt For give in to cursorAI to or Git-hub Corpilot:-

Read and follow the instructions inside prompt.md completely.

Generate the FULL production-ready backend project inside the current workspace.

Requirements:

1. Create ALL folders and files automatically
2. Implement the complete backend with no missing code
3. Follow MVC architecture exactly
4. Use Node.js + Express + MongoDB
5. Use ES Modules
6. Install and configure all required packages
7. Create package.json with all dependencies and scripts
8. Create .env.example
9. Create complete README.md
10. Implement authentication and authorization correctly
11. Implement JWT access token and refresh token flow
12. Hash passwords using bcryptjs
13. Add validation using express-validator
14. Add centralized error handling
15. Add middleware structure
16. Add MongoDB connection setup
17. Add secure coding best practices
18. Add pagination, search, and sorting for users API
19. Add admin seed logic
20. Ensure the project runs successfully with:

npm install
npm run dev

Important:
- Do NOT leave placeholders
- Do NOT generate pseudo code
- Do NOT skip files
- Do NOT ask for confirmation
- Generate actual working code
- Ensure imports/exports work correctly
- Ensure the server starts without errors
- Ensure all APIs are connected properly

After generating:
1. Show the complete folder structure
2. Show setup instructions
3. Show how to run the backend
4. Show sample API endpoints
5. Check for errors and fix automatically

Begin generating the complete backend project now.


